# Runtime dependencies used by Dockerfile and local FastAPI execution.
# With Docker, you do not need to run `pip install -r requirements.txt` manually:
# `docker compose up -d --build` installs these inside the ai-server image.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
redis==5.2.1
boto3==1.43.36
numpy==2.2.1
sqlite-vec==0.1.6
lightgbm==4.5.0
ortools==9.11.4210
pytest==8.3.4
httpx==0.28.1
